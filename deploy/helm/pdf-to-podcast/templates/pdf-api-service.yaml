{{- if .Values.pdfApi.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: pdf-api
  labels:
    app: {{ .Values.pdfApi.fullnameOverride }}
    {{- include "pdf-to-podcast.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Values.pdfApi.fullnameOverride }}
  type: {{ include "pdf-to-podcast.serviceType" . }}
  ports:
    - port: {{ .Values.pdfApi.service.port }}
      targetPort: http
      protocol: TCP
      name: http
{{- end }}
